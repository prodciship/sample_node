# Language setting
language: node_js

# Version number
node_js:
  - 0.12

build:
  pre_ci_boot:
    image_name: drydock/u12nod
    image_tag: tip
    pull: false
    options: '--privileged=true --net=bridge'
  
  ci:
    - echo "testing"
#  post_ci:
    # Always pull the latest base image to avoid any cached images on the host
 #   - docker build --rm -t kanthchandra/u12node:tip .
#   on_success:
#     - sudo mongo test --eval "db.dropDatabase()"

#   on_failure:
#     - sudo mongo test --eval "db.dropDatabase()"

integrations:
  notifications:
    - integrationName: irc
        type: irc
        recipients:
          - "chat.freenode.net#ck-rc-text"
        on_success: always
        on_start: always
        on_pull_request: always
        on_failure: always
